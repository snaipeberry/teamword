node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store
logs/

# artefact de build TypeScript : ne doit pas etre versionne
tsconfig.tsbuildinfo

# Données d'exécution du serveur temps réel (comptes, jetons de session,
# profils) : jamais versionnées.
data/
server/data/
